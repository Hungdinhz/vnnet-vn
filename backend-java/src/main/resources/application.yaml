server:
  port: 8000

spring:
  application:
    name: backend-java
  datasource:
    url: jdbc:postgresql://localhost:5432/vnnet
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# JWT Configuration
jwt:
  secret: who_am_i_vnnet_api
  expiration: 1800000  # 30 phút (milliseconds)

# Cloudinary Configuration
cloudinary:
  cloud-name: dmrfw1vw5
  api-key: "537557692366912"
  api-secret: KOEPZChTcB1T2BY4G5vzTHOlBiE
